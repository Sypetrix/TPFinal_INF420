Primos
O seguinte programa foi escrito para gerar uma lista de números primos. Após ler dois valores inteiros A e B, ele deveria gerar todos os números primos entre A e B, inclusive.
,
                stringstyle=,
                commentstyle=,
                morecomment=[l][]#,
                basewidth = .48em,
                showstringspaces=false
#include <iostream>
using namespace std;
int main()
    int a, b;
    cin >> a >> b;
    cout << "Primos entre " << a << " e " << b << ":";
    for(int i = a; i <= b; i++)
        if (primo(i))           //Se for primo
            cout << " " << i;   //imprime o valor
    cout << endl;
    return 0;
O problema é que não existe a função  usada neste programa. Sua tarefa é completar o programa criando esta função. A função deve seguir o cabeçalho abaixo. Ela recebe um número inteiro e retorna true/false caso o número recebido seja primo ou não[Se fizer na linguagem C, a função deve ser  e retornar 1 ou 0, pois em C não há .].
Entrada:
A entrada contém dois valores inteiros, A e B. Restrição: 1 ≤ A ≤ B ≤ 1000.
Saída:
Seu programa deve gerar apenas uma linha de saída, contendo a lista de primos entre A e B, escritos conforme os exemplos a seguir.
Nesta questão, seu programa deve usar a função  dada acima exatamente como está, não a modifique! Sua tarefa é apenas criar a função .